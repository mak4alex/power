$("<%=j render @posts %>").appendTo($("#posts"));

<% if @posts.last_page? %>
  $('.pagination').html("That's all, folks!");
<% else %>
  $('.pagination')
    .html("<%=j render partial: 'next_page_link', locals: { posts: @posts } %>");
<% end %>
